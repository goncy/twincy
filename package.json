{
  "name": "@goncy/twitch-chat-runner",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prebootstrap": "npm install",
    "bootstrap": "lerna bootstrap",
    "prestart": "npm run bootstrap",
    "start": "lerna run dev --parallel --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
