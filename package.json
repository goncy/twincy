{
  "name": "@goncy/twincy-runner",
  "version": "1.1.0",
  "license": "MIT",
  "author": "Goncy",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "kill": "kill-port --port 6600,6601,6602",
    "build": "lerna run build --parallel --stream",
    "build:tauri": "lerna run tauri -- build",
    "start": "lerna run start --parallel --stream",
    "predev": "npm run kill",
    "dev": "lerna run dev --parallel --stream",
    "dev:tauri": "lerna run tauri -- dev"
  },
  "devDependencies": {
    "kill-port": "^1.6.1",
    "lerna": "^4.0.0"
  }
}
